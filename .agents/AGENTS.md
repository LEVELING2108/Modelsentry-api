# Project Rules

- When committing changes to GitHub, always push to a feature branch and create a Pull Request (PR) instead of committing directly to the `main` branch. The user will manually review and merge it.
